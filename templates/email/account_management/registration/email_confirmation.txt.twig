{{ "general.say_hello" | trans({'name': name}, 'email', locale) }}

{{ 'account_management.email_confirmation.please_confirm_email' | trans({}, 'email', locale) }}

{{ signedUrl }}
{{ 'account_management.email_confirmation.link_expire_in' | trans({
    'time': expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle')
}, 'email', locale) }}
  
{{ 'general.say_bye' | trans({}, 'email', locale) | raw }}
