{{ "general.say_hello" | trans({}, 'email', locale) }}

{{ 'user.email_confirmation.please_confirm_email' | trans({}, 'email', locale) }}

{{ signedUrl }}
{{ 'user.email_confirmation.link_expire_in' | trans({
    'time': expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle', locale)
}, 'email', locale) }}
  
{{ 'general.say_bye' | trans({}, 'email', locale) | raw }}
