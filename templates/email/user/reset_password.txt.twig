{{ "general.say_hello" | trans({}, 'email', locale) }}

{{ 'user.reset_password.follow_the_link' | trans({}, 'email', locale) }}

{{ url('reset_password_reset', {
    token: resetToken.token,
    locale: locale
}) }}

{{ 'user.reset_password.link_expire_in' | trans({
    'time': resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle', locale)
}, 'email', locale) }}

{{ 'general.say_bye' | trans({}, 'email', locale)  | raw }}
